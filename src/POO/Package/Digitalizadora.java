package POO.Package;

// As interfaces funcionam como classes abstratas, mas com a diferença de que não podem ter métodos implementados.
// As interfaces são utilizadas para definir um contrato que outras classes devem seguir.
// A interface copiadora define um contrato que as classes que a implementam devem seguir.
// A interface copiadora possui um método chamado copiar que deve ser implementado pelas classes que a implementam.
public interface Digitalizadora {

    public void digitalizar();

}
